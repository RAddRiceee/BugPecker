<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.githubApp.dao.BugDao">

    <select id="getBugByIssue" resultType="java.lang.String">
        SELECT bug_location FROM BugLocation
        WHERE user_name = #{userName} and repo_name = #{repoName} and issue_id = #{issueId}
    </select>

    <select id="getIssueTitle" resultType="java.lang.String">
        SELECT issue_title FROM BugLocation
        WHERE user_name = #{userName} and repo_name = #{repoName} and issue_id = #{issueId}
    </select>

    <insert id="insertBugByIssue">
        insert into BugLocation (user_name,repo_name,issue_id,bug_location,issue_title) values (#{userName},#{repoName},#{issueId},#{bugLocation},#{issueTitle})
    </insert>

</mapper>